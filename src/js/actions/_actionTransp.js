import axios from 'axios';
import { apiPrefix } from './../../../etc/config.json';

export const currentMenuTransp = (value) => dispatch => {
    switch (value) {
        case 'toMyWg':
        case 'toMy':
        case 'newOrders':
        case 'carAppointed':
        case 'doneTrip':
        case 'cancelClient':

        case 'drivers':
        case 'cars':
        default:
    }
}